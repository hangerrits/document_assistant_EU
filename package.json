{
  "name": "chatbot-pdf-viewer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-pdf": "^7.4.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^4.2.4"
  },
  "scripts": {
    "generate-env": "node generate-react-env.js",
    "prebuild": "npm run generate-env",
    "build": "react-scripts build",
    "prestart": "npm run generate-env",
    "start": "react-scripts start",
    "view-env-log": "cat env-generation.log",
    "setup": "npm run generate-env && npm run view-env-log",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}